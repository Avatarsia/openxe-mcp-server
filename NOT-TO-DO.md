# NOT-TO-DO

Vermeidbare Fehlschritte — wird automatisch gefuellt. Siehe globale CLAUDE.md fuer Workflow.

| Datum | Tag | Fehler | Vermeidung |
|-------|-----|--------|------------|
| 2026-04-01 | api | Entity-Wrapper {auftrag: {...}} verursacht 500 — muss {data: {...}} sein | Legacy API: IMMER {data: {flat keys}} Format |
| 2026-04-01 | api | internebezeichnung falsch als internebemerkung implementiert | Exakter Feldname: internebezeichnung |
| 2026-04-01 | api | projekt-Feld mit numerischer ID — verursacht 500 | Projekt: String-Abkuerzung ("STANDARD"), KEINE numerische ID |
| 2026-04-01 | api | BelegEdit als funktionierend angenommen — crasht auf v1.12 | BelegEdit ist serverseitig broken, Fallback-Logik einbauen |
| 2026-04-01 | api | Einkaufspreise: 1 Tier = Object, 2+ = Array nicht normalisiert | Einkaufspreise IMMER zu Array normalisieren |
| 2026-04-01 | api | REST v1 fuer bestellung/einkaufspreise verwendet — gibt 404 | Beschaffung: ausschliesslich Legacy API |
| 2026-04-01 | api | BelegeList mit Filtern — Body-Parsing broken | BelegeList nicht fuer gefilterte Abfragen, ID-Iteration als Fallback |
| 2026-04-01 | mcp | Credentials in Skill-Datei hardcodiert statt .env | Credentials nur in .env, nie in Skills oder Configs |
| 2026-04-10 | api | OpenXE Legacy Create-Endpoints (AuftragCreate/AngebotCreate/RechnungCreate/GutschriftCreate) lieferten 7499 bei jedem Versuch | Payload muss FLAT sein (kein entity wrapper wie `{auftrag: ...}`), Positionen als `artikelliste.position`, und `kundennummer` (nicht nur `adresse`) ist Pflicht |
| 2026-04-10 | api | Sales-Belege mit Adresse ohne Kundennummer angelegt → 7499 statt klarer Fehlermeldung | Vor Create pruefen, dass `adresse.kundennummer` gesetzt ist; sonst client-side mit erklaerender Meldung abbrechen |
| 2026-04-10 | api | REST v1 `/v1/adressen` und `/v1/adressen/{id}` Responses sind doppelt genestet: `{data: {data: [...], pagination}}` | Immer `resp.data?.data ?? resp.data` verwenden, nicht einfach `resp.data` |
| 2026-04-14 | api | Reports/KPIs mit direktem `client.get(...)` + `Array.isArray(result.data)` lieferten live 0 Zeilen, weil REST v1 die Liste unter `.data.data` wrapped | Fuer Lese-Queries immer `fetchFilteredList` nutzen — der Helper normalisiert array vs. `{data:[]}`-Shapes und paginiert sauber |
| 2026-04-14 | api | KPIs/Reports/Business-Queries hatten harte `maxResults: 50/200/1000/5000` Caps und haben stumm abgeschnitten (z.B. `kunden-anzahl` gab ≤50 statt 2558 zurueck) | Fuer Zaehlungen/Summen immer `fetchAll: true` + `FETCH_ALL_SAFETY_CAP`-Signal. Handler muessen `meta.truncated` in `_warning` propagieren, nicht stillschweigend werfen |
| 2026-04-14 | api | Bestellungen-Fallback-Scan lief nur 1..200 und brach nach 3 Fehlschlaegen ab → verlor bei Luecken im ID-Raum Orders | OpenXE teilt sich den Beleg-ID-Zaehler ueber alle Typen; Purchase-Order-IDs sind sparse und hoch. Scan braucht `PURCHASE_ORDER_SCAN_CAP=5000` und `CONSECUTIVE_MISS_LIMIT=50`, und muss seine Obergrenze als `truncated` signalisieren |
| 2026-04-14 | api | `BelegeList typ=bestellung` (Legacy-Efficient-Fetch) funktioniert auf v1.12 nicht (liefert 7499 "Data not found") | Nicht als primaere Quelle annehmen — der Scan-Fallback in `fetchPurchaseOrders` muss produktionstauglich sein, nicht nur Notnagel |
| 2026-04-14 | config | MCP StreamableHTTPServerTransport wurde pro POST-Request neu erzeugt + `server.connect()` jedesmal erneut aufgerufen — keine Session-Kontinuitaet, keine SSE-Streams, Memory-Leak durch doppelte Handler-Registrierung | Kanonisches Pattern aus `servers/everything/streamableHttp.ts`: `Map<sessionId, Transport>`, POST differenziert Initialize vs. Follow-up via `isInitializeRequest`, GET/DELETE fuer SSE + Teardown, `server.connect()` genau einmal pro Session, `onclose` + Idle-Sweep gegen tote Sessions |
| 2026-04-14 | config | Modul-globaler Rate-Limit-Counter (`discoverCallLog: number[]`) wurde zwischen allen HTTP-Clients geteilt — ein Client konnte andere stummschalten | Session-scoped State gehoert in `Map<sessionId, Counter>` mit expliziter sessionId beim Dispatch. `AsyncLocalStorage` ist fuer request-scoped State, nicht session-scoped (ueberlebt ueber Requests) — semantisch falsch hier |
| 2026-04-14 | config | MCP-Server `instructions`-Block mit 125 Zeilen / ~2.500 Tokens wurde bei jeder Verbindung mitgesendet — ~10× Community-Median, bricht Attention kleiner lokaler LLMs (NoLiMa/RULER ab ~4-8k Tokens) | Offizielle Anthropic-Beispielserver setzen `instructions` meist auf leer. Tool-spezifische Regeln gehoeren in die jeweilige Tool-`description`. Kurzer modus-abhaengiger Kontext-Block reicht (router/full/readonly getrennt, jeweils <400 Tokens) |
| 2026-07-01 | api | OpenXE 7499 "Unexpected error" ist ein FALSE NEGATIVE bei create-*: ApiBelegCreate persistiert den Beleg VOR dem fehlerschlagenden Schritt (keine Transaktion) — Beleg existiert trotz Fehler. Blindes Retry erzeugt Duplikate (real: 10 Angebote aus 1 Vorgang) | Nach create-Fehler NIE plain-error zurueckgeben. Erst verifizieren (list belege nach kundennummer + Zeitfenster), dann definitives Outcome. OpenXE-seitig: Issue #18 (Transaktion+Rollback). Zusaetzlich Idempotenz-Cache gegen identische Retries |
| 2026-07-01 | api | 7499 wird von OpenXEs eigenem ErrorHandler abgefangen (ApiError::CODE_UNEXPECTED_ERROR) und der Trace hinter einem Debug-Flag unterdrueckt — steht in KEINEM Standard-Log (php error_log=SAPI, kein Write im catch) | Root-Cause von 7499 nur via Instrumentierung des catch in classes/Modules/Api/Error/ErrorHandler.php (~Z.135) ermittelbar. Nicht im apache/php-Log suchen |
